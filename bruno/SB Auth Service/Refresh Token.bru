meta {
  name: Refresh Token
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/api/auth/refreshtoken
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{jwt}}
}

body:json {
  {
      "refreshToken": "{{refreshToken}}"
  }
}
